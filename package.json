{
  "name": "seekmeal-mobile",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd mobile && npx expo start",
    "android": "cd mobile && npx expo run:android",
    "ios": "cd mobile && npx expo run:ios"
  }
}
